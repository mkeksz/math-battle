interface Expression {
  question: string,
  answer: string
}

interface LimitsExpression {
  maxNumber: number,
  minNumber: number
}

interface Character {
  healthPoints: number,
  attackPower: number
}
